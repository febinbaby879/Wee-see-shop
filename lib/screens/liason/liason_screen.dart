import 'package:flutter/material.dart';

class LiasonScreen extends StatelessWidget {
  const LiasonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LiasonScreen'),
      ),
    );
  }
}
